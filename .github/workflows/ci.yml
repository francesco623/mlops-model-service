name: ci

on:
    push:
        
    pull_request:
        branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: download repo
              uses: actions/checkout@v4

            - name: run tests
              run: docker compose run --rm test
